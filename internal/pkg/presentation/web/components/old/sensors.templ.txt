/*
package components

import (
	"context"
	"fmt"
	"github.com/diwise/diwise-web/internal/pkg/presentation/locale"
	"github.com/diwise/diwise-web/internal/pkg/presentation/web/assets"
	"io"
	"regexp"
	"time"
)

type SensorViewModel struct {
	Active       bool
	DevEUI       string
	DeviceID     string
	Name         string
	BatteryLevel int
	LastSeen     time.Time
	HasAlerts    bool
}

type StatisticsViewModel struct {
	Total    int
	Active   int
	Inactive int
	Online   int
	Unknown  int
}

type SensorListViewModel struct {
	Sensors    []SensorViewModel
	Statistics StatisticsViewModel
}

templ SearchField(l10n locale.Localizer) {
	<div class="w-[237px] h-12 flex-col justify-center items-start gap-2 inline-flex">
		<div class="self-stretch pl-5 pr-4 py-3 bg-white bg-opacity-50 rounded-xl border border-input-surface border-opacity-50 justify-start items-center gap-2 inline-flex dark:bg-gray-800 dark:border-zinc-600">
			<div class="w-[22px] h-[22px]">
				<svg class="w-[22px] h-[22px] fill-secondary-dark dark:fill-zinc-600">
					@templ.Raw(iconSVG("search"))
				</svg>
			</div>
			<div class="grow shrink basis-0 text-input-surface text-opacity-30 text-lg font-normal font-sans leading-normal inline-flex">
				<input
					class="w-[175px] rounded-xl bg-white dark:bg-gray-800 dark:rounded-none dark:text-white dark:font-normal"
					type="search"
					placeholder={ l10n.Get("search") }
				/>
			</div>
		</div>
	</div>
}

templ SumOfStuff(l10n locale.Localizer, stats StatisticsViewModel) {
	<div class="flex items-start gap-10 dark:text-white">
		<div class="flex items-start gap-2">
			{ l10n.Get("total") }
			<span class="font-bold">{ fmt.Sprintf("%d", stats.Total) }</span>
		</div>
		<div class="flex items-start gap-2">
			{ l10n.Get("manyactive") }
			<span class="font-bold">{ fmt.Sprintf("%d", stats.Active) }</span>
		</div>
		<div class="flex items-start gap-2">
			{ l10n.Get("manyinactive") }
			<span class="font-bold">{ fmt.Sprintf("%d", stats.Inactive) }</span>
		</div>
		<div class="flex items-start gap-2">
			{ l10n.Get("unknown") }
			<span class="font-bold">{ fmt.Sprintf("%d", stats.Unknown) }</span>
		</div>
	</div>
}

templ SensorTable(l10n locale.Localizer, asset assets.AssetLoaderFunc, sensors SensorListViewModel) {
	@Table(
		HeaderRow(TableHeaderText(l10n.Get("name")), TableHeaderText(l10n.Get("status")), TableHeaderText(l10n.Get("deveui")), TableHeaderText(l10n.Get("batterylevel")), TableHeaderText(l10n.Get("lastseen"))),
		TableData(tableRowsFromSensors(l10n, asset, sensors.Sensors)...),
		DataTableFooter("6", "sensors"), l10n,
	)
}

func tableMapSwitch(id, name, icon string) templ.Component {

	return templ.ComponentFunc(func(ctx context.Context, w io.Writer) error {
		_, err := io.WriteString(w, fmt.Sprintf(
			`
                <button id="%s-button" class="h-[30px] px-3.5 py-1.5 rounded-xl justify-center items-center gap-2 flex cursor-pointer inactive"
                    hx-get="?view=%s"
					hx-target="#tableOrMap"
                    hx-push-url="true"
                    hx-trigger="click"
					hx-swap="innerHTML"
					hx-select="#%s"
					hx-on="click: this.classList.add('active'); this.classList.remove('inactive'); 
                           document.getElementById('%s-button').classList.add('inactive'); 
                           document.getElementById('%s-button').classList.remove('active');">
                    <svg class="w-[18px] h-[18px]"> 
                        %s
                    </svg>
                    <div class="text-sm font-bold font-sans leading-[18px]">%s</div>
                </button>
            `, id, id, id, id, id, icon, name,
		))
		return err
	})
}

templ tooltip(tooltiptext string) {
	<div class="absolute top-full left-1/2 transform -translate-x-1/2 mt-2 hidden group-hover:block">
		<svg class="w-[40px] h-[8px] absolute top-0 left-1/2 transform -translate-x-1/2 -translate-y-full">
			@SVG("tip", Box(41, 8), FillColor("black", "white"))
		</svg>
		<div class="bg-primary-surface text-white dark:bg-white dark:text-black rounded-lg shadow-lg flex py-2 px-4 items-center gap-1.5 text-sm font-bold font-sans leading-[18px]">
			{ tooltiptext }
		</div>
	</div>
}

templ mapTableButtons(l10n locale.Localizer) {
	<div class="flex items-center self-stretch gap-6">
		<div class="flex p-2 items-center gap-4 bg-tertiary-surface bg-opacity-20 dark:bg-primary-surface-white rounded-2xl gap-2">
			/*@tableMapSwitch("list", "Lista", iconSVG("list"))
			  @tableMapSwitch("mapview", "Karta", iconSVG("map"))*/
			<div class="relative group">
				<button
					id="tableButton"
					class="flex p-1.5 rounded-[10px] justify-center items-center gap-1 cursor-pointer bg-black fill-white dark:bg-white dark:fill-black stroke-white dark:stroke-black hover:bg-tertiary-surface-hover"
					hx-get="?view=table"
					hx-target="#tableOrMap"
					hx-push-url="true"
					hx-trigger="click"
					hx-swap="innerHTML"
					hx-select="#table"
					hx-on="click: this.classList.add('bg-black', 'fill-white', 'dark:bg-white', 'dark:fill-black', 'stroke-white'); this.classList.remove('dark:fill-white', 'stroke-black'); 
					document.getElementById('mapButton').classList.add('fill-black', 'dark:fill-white'); 
					document.getElementById('mapButton').classList.remove('bg-black', 'fill-white', 'dark:bg-white', 'dark:fill-black');"
				>
					<svg class="w-[18px] h-[18px]">
						@SVG("table", Size(24), NoFill(), Box(24, 24), Stroke(templ.Attributes{"stroke-width": "2", "stroke-linecap": "round", "stroke-linejoin": "round"}))
					</svg>
				</button>
				@tooltip(l10n.Get("table"))
			</div>
			<div class="relative group">
				<button
					id="mapButton"
					class="flex p-1.5 rounded-[10px] justify-center items-center gap-1 cursor-pointer dark:fill-white hover:bg-tertiary-surface-hover"
					hx-get="?view=mapview"
					hx-target="#tableOrMap"
					hx-push-url="true"
					hx-trigger="click"
					hx-swap="innerHTML"
					hx-select="#mapview"
					hx-on="click: this.classList.add('bg-black', 'fill-white', 'dark:bg-white', 'dark:fill-black' ); this.classList.remove('dark:fill-white'); 
					document.getElementById('tableButton').classList.add('fill-black', 'dark:fill-white', 'stroke-black'); 
					document.getElementById('tableButton').classList.remove('bg-black', 'fill-white', 'dark:bg-white', 'dark:fill-black', 'stroke-white');"
				>
					<svg class="w-[18px] h-[18px]">
						@templ.Raw(iconSVG("map"))
					</svg>
				</button>
				@tooltip(l10n.Get("map"))
			</div>
		</div>
	</div>
}

templ Sensors(l10n locale.Localizer, asset assets.AssetLoaderFunc, sensors SensorListViewModel) {
	<div class="flex flex-col items-start gap-14 flex-[1_0_0] py-8 w-full" id="sensor-view">
		<div class="flex flex-col items-start gap-6 self-stretch w-full px-8">
			<div class="">
				<h1 class="dark:text-white text-2xl font-bold font-heading leading-loose">{ l10n.Get("sensors") }</h1>
			</div>
			@SumOfStuff(l10n, sensors.Statistics)
		</div>
		<div class="h-px border-t border-gray-30 w-full dark:border-white-30"></div>
		<div class="flex px-8 flex-col items-start self-stretch gap-10">
			<div class="w-full h-12 justify-between items-center inline-flex">
				<div class="inline-flex justify-start items-center gap-[34px] dark:text-white">
					//@SearchField(l10n)
					@FilterSensors(l10n)
				</div>
				//@Button(Primary, l10n.Get("addthing"), "", HxUrl("get", (fmt.Sprintf("/components/sensors"))))
				
				@mapTableButtons(l10n)
			</div>
			<div id="tableOrMap" class="w-full flex flex-col gap-6 items-start inline-flex">
				//Initialt tabellinnehåll
				<div id="sensors-table" class="w-full">
					@SensorTable(l10n, asset, sensors)
				</div>
			</div>
			//Innehåll dolt, hämtas när man klickar på Lista/Karta
			<div id="fragments" style="display: none;">
				<div id="table" class="w-full">
					<div id="sensors-table" class="w-full">
						@SensorTable(l10n, asset, sensors)
					</div>
				</div>
				<div id="mapview" class="w-full">
					@Map(newMapData(62.3908, 17.3069), l10n)
				</div>
			</div>
		</div>
	</div>
}

//HELPERS
func tableRowsFromSensors(l10n locale.Localizer, _ assets.AssetLoaderFunc, sensors []SensorViewModel) []templ.Component {
	rows := []templ.Component{}

	labelText := map[bool]string{
		false: l10n.Get("inactive"),
		true:  l10n.Get("active"),
	}

	for _, sensor := range sensors {
		isActive := sensor.Active

		cells := []templ.Component{TextFirstCell(sensor.Name)}
		cells = append(cells, LabelCell(labelText[isActive], isActive))
		cells = append(cells, TextCell(sensor.DevEUI))
		//cells = append(cells, TextCell(sensor.net))

		cells = append(cells, BatteryTableCell(sensor))

		if sensor.LastSeen.IsZero() {
			cells = append(cells, TextCell(""))
		} else {
			cells = append(cells, TextCell(sensor.LastSeen.Format("2006-01-02, 15:04")))
		}

		/*if sensor.HasAlerts {
			cells = append(cells, Cell(SVG("alert-triangle", Size(24), AlertStyle())))
		} else {
			cells = append(cells, TextCell(""))
		}*/

		linkUrl := templ.URL(fmt.Sprintf("/components/sensors/details?id=%s", sensor.DeviceID))
		pushUrl := templ.URL(fmt.Sprintf("/sensors/%s", sensor.DeviceID))

		row := LinkRow(cells, linkUrl, pushUrl, "sensor")
		rows = append(rows, row)
	}

	return rows
}

var re = regexp.MustCompile(`[^a-zA-Z0-9]`)

func BatteryTableCell(sensor SensorViewModel) templ.Component {
	selector := "batlevel_" + re.ReplaceAllString(sensor.DeviceID, "")
	return ActiveCell(Text("-"), selector, fmt.Sprintf("/components/sensors/%s/batterylevel", sensor.DeviceID), "load, every 10m")
}

templ CustomCheckbox() {
	<div class="w-[20px] h-[20px] bg-white rounded border border-gray-400 peer-checked:bg-black peer-checked:border-black flex justify-center items-center">
		<svg class="w-full h-full fill-none stroke-white stroke-2 opacity-100 peer-checked:opacity-0 transition-opacity" viewBox="0 0 24 24">
			@templ.Raw(iconSVG("check"))
		</svg>
	</div>
}

templ CheckboxButton(name string, l10n locale.Localizer) {
	<button class="flex items-center justify-start text-left p-2 gap-2 font-bold hover:bg-tertiary-surface-hover hover:rounded-lg">
		{ l10n.Get( name ) }
		<svg class="w-[18px] h-[18px] dark:fill-white">
			@templ.Raw(iconSVG("chevron-down"))
		</svg>
	</button>
}

templ CheckboxList(id, target, hxInclude, hxGet, name string, options []string, l10n locale.Localizer) {
	<div class="hidden absolute bg-gray-100 min-w-[160px] rounded-md shadow-lg z-10 p-4 group-hover:block">
				<form id={ id } hx-trigger="change" hx-target={ target } hx-include={ hxInclude } hx-get={ hxGet } hx-push-url="true" class="flex flex-col gap-1">
			for _, option := range options {
				<label class="flex justify-between items-center">
					{ l10n.Get( option ) }
					<input type="checkbox" name={ name } value={ option } class="sr-only peer form-checkbox cursor-pointer">
					@CustomCheckbox()
				</label>
			}
		</form>
	</div>
}

templ FilterSensors(l10n locale.Localizer) {
	<div class="flex items-center flex-[1_0_0] gap-6">
		<div class="relative inline-block group">
			@CheckboxButton("status", l10n)
			@CheckboxList("statusForm", "#fragments", "#tenantForm", "/sensors", "status", []string{"active", "inactive", "unknown"}, l10n)
		</div>

		<div class="relative inline-block group">
			@CheckboxButton("organisation", l10n)
			@CheckboxList("tenantForm", "#fragments", "#statusForm", "/sensors", "tenant", []string{"MSVA", "Servanet", "Default"}, l10n)
		</div>


		//Sök och dropdown, for the future
		/*<form hx-push-url="true" hx-trigger="change" hx-get="?" hx-vals='{"filter": this.value}'>
			<input list="filter-options" name="filter" placeholder="Select filter">
			<datalist id="filter-options">
				<option value="all">All</option>
				<option value="active">Active</option>
				<option value="inactive">Inactive</option>
				<option value="unknown">Unknown</option>
			</datalist>
		</form>*/

	</div>
}
